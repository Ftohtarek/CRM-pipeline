import { SlideOnDragDirective } from './slide-on-drag.directive';

describe('SlideOnDragDirective', () => {
  it('should create an instance', () => {
    const directive = new SlideOnDragDirective();
    expect(directive).toBeTruthy();
  });
});
